server:
  port: 8080

spring:
  profiles:
    active: mysql
  application:
    name: user-service

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: cn.edu.usc.quzhijie.userservice.entity
  configuration:
    map-underscore-to-camel-case: true